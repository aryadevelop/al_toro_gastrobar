$kind: http-request
name: 04 MESERO obtiene el borrador armado – 200 OK
description: |-
  El borrador ahora contiene los 2 platos de carta agregados desde cero.
  Captura `tmpComandaCocinaId` (para enviar/producir) y `tmpItemEliminar` (un comandaItemId para eliminar en el cierre).
url: "{{baseUrl}}/api/comandas/borrador?visitaId={{tmpVisitaId}}"
method: GET
headers:
  Authorization: Bearer {{meseroToken}}
scripts:
  - type: beforeRequest
    code: |-
      pm.sendRequest({
        url: pm.environment.get('baseUrl') + '/api/auth/login',
        method: 'POST',
        header: { 'Content-Type': 'application/json' },
        body: { mode: 'raw', raw: JSON.stringify({
          email: pm.environment.get('emailMesero'),
          password: pm.environment.get('passwordValida'),
          forceSessionOverride: true
        })}
      }, (err, res) => {
        if (!err && res && res.code === 200) {
          const token = res.json().accessToken;
          pm.environment.set('meseroToken', token);
          pm.request.headers.upsert({ key: 'Authorization', value: 'Bearer ' + token });
        }
      });
    language: text/javascript
  - type: afterResponse
    code: |-
      pm.test('El sistema retorna HTTP 200', function () {
        pm.response.to.have.status(200);
      });
      pm.test('El borrador tiene los 2 platos armados desde cero', function () {
        const data = pm.response.json().data;
        pm.expect(data.platos, 'platos').to.be.an('array').with.lengthOf.at.least(2);
        pm.expect(data.comandaCocinaId, 'comandaCocinaId').to.be.a('number');
        pm.environment.set('tmpComandaCocinaId', data.comandaCocinaId);
        // Captura el comandaItemId del primer plato para eliminarlo después en la cuenta
        pm.environment.set('tmpItemEliminar', data.platos[0].comandaItemId);
      });
    language: text/javascript
order: 130
