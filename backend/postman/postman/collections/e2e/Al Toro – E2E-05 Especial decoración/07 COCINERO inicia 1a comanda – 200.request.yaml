$kind: http-request
name: 07 COCINERO inicia la 1ª comanda – 200 OK
description: La 1ª comanda COCINA pasa PENDIENTE → EN_PREPARACION. Queda en producción mientras se arma la 2ª tanda.
url: "{{baseUrl}}/api/comandas/produccion/{{tmpComandaCocinaId}}/iniciar"
method: POST
headers:
  Authorization: Bearer {{cocineroToken}}
scripts:
  - type: beforeRequest
    code: |-
      pm.sendRequest({
        url: pm.environment.get('baseUrl') + '/api/auth/login',
        method: 'POST',
        header: { 'Content-Type': 'application/json' },
        body: { mode: 'raw', raw: JSON.stringify({
          email: pm.environment.get('emailCocinero'),
          password: pm.environment.get('passwordValida'),
          forceSessionOverride: true
        })}
      }, (err, res) => {
        if (!err && res && res.code === 200) {
          const token = res.json().accessToken;
          pm.environment.set('cocineroToken', token);
          pm.request.headers.upsert({ key: 'Authorization', value: 'Bearer ' + token });
        }
      });
    language: text/javascript
  - type: afterResponse
    code: |-
      pm.test('El sistema inicia la 1ª comanda (HTTP 200)', function () {
        pm.response.to.have.status(200);
      });
      pm.test('La 1ª comanda queda EN_PREPARACION', function () {
        pm.expect(pm.response.json().data.comandaEstado).to.eql('EN_PREPARACION');
      });
    language: text/javascript
order: 160
