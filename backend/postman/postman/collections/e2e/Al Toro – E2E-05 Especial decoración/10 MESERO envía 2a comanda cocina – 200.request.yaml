$kind: http-request
name: 10 MESERO envía la 2ª comanda COCINA – 200 OK
description: Transiciona la 2ª comanda COCINA BORRADOR → PENDIENTE, coexistiendo con la 1ª (EN_PREPARACION).
url: "{{baseUrl}}/api/comandas/borrador/{{tmpComandaCocinaId2}}/enviar"
method: POST
headers:
  Authorization: Bearer {{meseroToken}}
scripts:
  - type: beforeRequest
    code: |-
      pm.sendRequest({
        url: pm.environment.get('baseUrl') + '/api/auth/login',
        method: 'POST',
        header: { 'Content-Type': 'application/json' },
        body: { mode: 'raw', raw: JSON.stringify({
          email: pm.environment.get('emailMesero'),
          password: pm.environment.get('passwordValida'),
          forceSessionOverride: true
        })}
      }, (err, res) => {
        if (!err && res && res.code === 200) {
          const token = res.json().accessToken;
          pm.environment.set('meseroToken', token);
          pm.request.headers.upsert({ key: 'Authorization', value: 'Bearer ' + token });
        }
      });
    language: text/javascript
  - type: afterResponse
    code: |-
      pm.test('El sistema envía la 2ª comanda (HTTP 200)', function () {
        pm.response.to.have.status(200);
      });
    language: text/javascript
order: 190
