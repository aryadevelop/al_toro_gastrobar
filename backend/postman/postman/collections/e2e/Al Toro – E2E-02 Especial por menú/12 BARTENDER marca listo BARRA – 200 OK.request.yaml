$kind: http-request
name: 12 BARTENDER marca la comanda BARRA lista – 200 OK
description: La comanda BARRA pasa EN_PREPARACION → LISTO (genera notificación BEBIDAS_LISTAS).
url: "{{baseUrl}}/api/comandas/produccion/{{tmpComandaBarraId}}/listo"
method: POST
headers:
  Authorization: Bearer {{bartenderToken}}
scripts:
  - type: beforeRequest
    code: |-
      pm.sendRequest({
        url: pm.environment.get('baseUrl') + '/api/auth/login',
        method: 'POST',
        header: { 'Content-Type': 'application/json' },
        body: { mode: 'raw', raw: JSON.stringify({
          email: pm.environment.get('emailBartender'),
          password: pm.environment.get('passwordValida'),
          forceSessionOverride: true
        })}
      }, (err, res) => {
        if (!err && res && res.code === 200) {
          const token = res.json().accessToken;
          pm.environment.set('bartenderToken', token);
          pm.request.headers.upsert({ key: 'Authorization', value: 'Bearer ' + token });
        }
      });
    language: text/javascript
  - type: afterResponse
    code: |-
      pm.test('El sistema marca la comanda BARRA lista (HTTP 200)', function () {
        pm.response.to.have.status(200);
      });
      pm.test('La comanda BARRA queda LISTO', function () {
        pm.expect(pm.response.json().data.comandaEstado).to.eql('LISTO');
      });
    language: text/javascript
order: 210
