$kind: http-request
name: MI-06 Reserva PENDIENTE – 422 Unprocessable Entity
url: "{{baseUrl}}/api/reservas/11/marcar-inasistencia"
method: PATCH
headers:
  Authorization: Bearer {{meseroToken}}
scripts:
  - type: beforeRequest
    code: |-
      pm.sendRequest({
        url: pm.environment.get('baseUrl') + '/api/auth/login',
        method: 'POST',
        header: { 'Content-Type': 'application/json' },
        body: { mode: 'raw', raw: JSON.stringify({
          email: pm.environment.get('emailMesero'),
          password: pm.environment.get('passwordValida'),
          forceSessionOverride: true
        })}
      }, function (err, res) {
        if (!err && res && res.code === 200) {
          pm.environment.set('meseroToken', res.json().accessToken);
        } else {
          console.warn('MI-06: login MESERO falló', err, res && res.code);
        }
      });
    language: text/javascript
  - type: afterResponse
    code: |-
      pm.test('Reserva PENDIENTE → 422', function () {
        pm.response.to.have.status(422);
      });

      pm.test('Código NEG-002 y mensaje indica estado', function () {
        const body = pm.response.json();
        pm.expect(body.success).to.be.false;
        pm.expect(body.code).to.equal('NEG-002');
        pm.expect(body.message).to.include('confirmadas');
      });
    language: text/javascript
order: 6000
