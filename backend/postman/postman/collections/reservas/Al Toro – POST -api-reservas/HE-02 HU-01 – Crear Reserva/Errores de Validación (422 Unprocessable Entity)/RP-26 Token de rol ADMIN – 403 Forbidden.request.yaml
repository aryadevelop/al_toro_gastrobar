$kind: http-request
description: |-
  **Criterio de Aceptación:** RP-26
  **Objetivo:** Verificar que el endpoint de creación de reservas deniega el acceso cuando el token corresponde al rol ADMIN. El endpoint solo permite el rol CLIENTE.
  **Pre-condición:** `accessToken` (ADM) disponible.
  **Resultado esperado:** HTTP 403 con `success=false`.
url: "{{baseUrl}}/api/reservas"
method: POST
headers:
  Content-Type: application/json
  Authorization: Bearer {{accessToken}}
body:
  type: text
  content: |-
    {
      "emailCliente": "{{emailCliente}}",
      "fechaHoraLlegada": "{{fechaLibre}}",
      "numeroPersonas": 3
    }
scripts:
  - type: afterResponse
    code: |-
      pm.test('Status 403 – Rol ADMIN no puede crear reservas', () => pm.response.to.have.status(403));

      pm.test('El campo success es false en la respuesta de error', () => pm.expect(pm.response.json().success).to.be.false);
    language: text/javascript
order: 3000
