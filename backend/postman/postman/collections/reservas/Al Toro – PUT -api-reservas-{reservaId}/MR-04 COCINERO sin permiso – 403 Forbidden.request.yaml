$kind: http-request
name: MR-04 COCINERO sin permiso – 403 Forbidden
url: "{{baseUrl}}/api/reservas/1"
method: PUT
headers:
  Authorization: Bearer {{cocineroToken}}
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "fechaHoraLlegada": "2028-06-15T19:00:00",
      "numeroPersonas": 2
    }
scripts:
  - type: beforeRequest
    code: |-
      pm.sendRequest({
        url: pm.environment.get('baseUrl') + '/api/auth/login',
        method: 'POST',
        header: { 'Content-Type': 'application/json' },
        body: { mode: 'raw', raw: JSON.stringify({
          email: pm.environment.get('emailCocinero'),
          password: pm.environment.get('passwordValida'),
          forceSessionOverride: true
        })}
      }, function (err, res) {
        if (!err && res && res.code === 200) {
          pm.environment.set('cocineroToken', res.json().accessToken);
        } else {
          console.warn('MR-04: login COCINERO falló', err, res && res.code);
        }
      });
    language: text/javascript
  - type: afterResponse
    code: |-
      pm.test('COCINERO no puede modificar reservas → 403', function () {
        pm.response.to.have.status(403);
      });
    language: text/javascript
order: 4000
