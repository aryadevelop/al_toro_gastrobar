$kind: http-request
name: CU-01 Sin token – 401 Unauthorized
url: "{{baseUrl}}/api/ventas/1/cuenta"
method: GET
scripts:
  - type: afterResponse
    code: |-
      pm.test('Sin token → 401', function () {
        pm.response.to.have.status(401);
      });
    language: text/javascript
order: 1000
