$kind: http-request
name: PB-02 CLIENTE sin permiso – 403 Forbidden
description: |-
  **Criterio de Aceptación:** Matriz de roles — endpoint exclusivo de `CAJERO`/`ADMIN`.
  **Objetivo:** Verificar que `@PreAuthorize("hasAnyRole('CAJERO','ADMIN')")` deniega token
  de CLIENTE (el cliente debe usar `/me/puntos`).
  **Pre-condición:** El `beforeRequest` de este test hace login autónomo para obtener
  `clienteToken`, de modo que no depende del hook de colección.
  **Resultado esperado:** 403 Forbidden.
url: "{{baseUrl}}/api/clientes/1/puntos"
method: GET
headers:
  Authorization: Bearer {{clienteToken}}
scripts:
  - type: beforeRequest
    code: |-
      pm.sendRequest({
        url: pm.environment.get('baseUrl') + '/api/auth/login',
        method: 'POST',
        header: { 'Content-Type': 'application/json' },
        body: {
          mode: 'raw',
          raw: JSON.stringify({
            email: pm.environment.get('emailCliente'),
            password: pm.environment.get('passwordValida'),
            forceSessionOverride: true
          })
        }
      }, function (err, res) {
        if (!err && res && res.code === 200) {
          const token = res.json().accessToken;
          pm.environment.set('clienteToken', token);
          pm.request.headers.upsert({ key: 'Authorization', value: 'Bearer ' + token });
        } else {
          console.warn('PB-02: login CLIENTE falló', err, res && res.code);
        }
      });
    language: text/javascript
  - type: afterResponse
    code: |-
      pm.test('El sistema rechaza token de CLIENTE con 403', function () {
        pm.response.to.have.status(403);
      });
    language: text/javascript
order: 2000
