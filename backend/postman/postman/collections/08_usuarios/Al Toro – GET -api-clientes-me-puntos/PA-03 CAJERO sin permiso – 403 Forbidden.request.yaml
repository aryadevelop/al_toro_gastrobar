$kind: http-request
name: PA-03 CAJERO sin permiso – 403 Forbidden
description: |-
  **Criterio de Aceptación:** Matriz de roles — el endpoint es exclusivo de `CLIENTE`.
  **Objetivo:** Verificar que `@PreAuthorize("hasRole('CLIENTE')")` deniega token de CAJERO.
  **Pre-condición:** El `beforeRequest` de este test hace login autónomo para obtener
  `cajeroToken`, independiente del hook de colección.
  **Resultado esperado:** 403 Forbidden.
url: "{{baseUrl}}/api/clientes/me/puntos?emailCliente={{emailCliente}}"
method: GET
headers:
  Authorization: Bearer {{cajeroToken}}
queryParams:
  emailCliente: "{{emailCliente}}"
scripts:
  - type: beforeRequest
    code: |-
      pm.sendRequest({
        url: pm.environment.get('baseUrl') + '/api/auth/login',
        method: 'POST',
        header: { 'Content-Type': 'application/json' },
        body: {
          mode: 'raw',
          raw: JSON.stringify({
            email: pm.environment.get('emailCajero'),
            password: pm.environment.get('passwordValida'),
            forceSessionOverride: true
          })
        }
      }, function (err, res) {
        if (!err && res && res.code === 200) {
          const token = res.json().accessToken;
          pm.environment.set('cajeroToken', token);
          pm.request.headers.upsert({ key: 'Authorization', value: 'Bearer ' + token });
        } else {
          console.warn('PA-03: login CAJERO falló', err, res && res.code);
        }
      });
    language: text/javascript
  - type: afterResponse
    code: |-
      pm.test('El sistema rechaza token de CAJERO con 403', function () {
        pm.response.to.have.status(403);
      });
    language: text/javascript
order: 3000
