$kind: http-request
description: |-
  **Criterio de Aceptación:** CA-18
  **Objetivo:** Verificar que no se permite cambiar a una contraseña que no cumple con requisitos de fuerza (< 8 caracteres o sin mayúscula/minúscula/número/símbolo).
url: "{{baseUrl}}/api/clientes/me/cambiar-contraseña"
method: POST
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "contraseñaActual": "ClientePass123!",
      "nuevaContraseña": "weak",
      "confirmacion": "weak"
    }
scripts:
  - type: beforeRequest
    code: |-
      // Login como CLIENTE para obtener token
      const loginRequest = {
        url: pm.environment.get('baseUrl') + '/api/auth/login',
        method: 'POST',
        header: {
          'Content-Type': 'application/json'
        },
        body: {
          mode: 'raw',
          raw: JSON.stringify({
            email: 'cliente@altoro.com',
            password: 'ClientePass123!'
          })
        }
      };

      pm.sendRequest(loginRequest, function (err, response) {
        if (!err && response.code === 200) {
          const token = response.json().accessToken;
          pm.request.headers.add({ key: 'Authorization', value: 'Bearer ' + token });
        }
      });
    language: text/javascript
  - type: afterResponse
    code: |-
      pm.test('Status 400 – Contraseña débil', () => pm.response.to.have.status(400));

      const body = pm.response.json();

      pm.test('Mensaje de error sobre requisitos de contraseña', () => {
        pm.expect(body.message).to.include('8 caracteres');
      });
    language: text/javascript
order: 3004

