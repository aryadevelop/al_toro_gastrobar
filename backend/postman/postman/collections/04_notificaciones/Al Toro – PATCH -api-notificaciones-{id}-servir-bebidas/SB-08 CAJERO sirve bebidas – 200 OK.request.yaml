$kind: http-request
name: SB-08 CAJERO sirve bebidas – 200 OK
description: |-
  > ⚠️ TODO — Test dependiente de seed data
  >
  > Requiere una notificación seed ACTIVA de tipo BEBIDAS_LISTAS (id 14).
  > Cuando se construya el endpoint público para marcar comanda BARRA como LISTO,
  > refactorizar a patrón create-fresh.

  **Objetivo:** Verificar que CAJERO también puede registrar bebidas servidas (PA-111).
  **Pre-condición:** `cajeroToken` disponible. Notificación seed ACTIVA BEBIDAS_LISTAS id=18.
  **Resultado esperado:** 200 OK.
url: "{{baseUrl}}/api/notificaciones/18/servir-bebidas"
method: PATCH
headers:
  Authorization: Bearer {{cajeroToken}}
scripts:
  - type: afterResponse
    code: |-
      pm.test('HTTP 200', () => pm.response.to.have.status(200));
    language: text/javascript
order: 7208
