$kind: http-request
name: SP-08 CAJERO sirve platos – 200 OK
description: |-
  > ⚠️ TODO — Test dependiente de seed data
  >
  > Requiere una notificación seed ACTIVA de tipo PLATOS_LISTOS (id `seedNotificacionPlatosListosId3`).
  > Cuando se construya el endpoint público para marcar comanda como LISTO, refactorizar
  > a patrón create-fresh.

  **Objetivo:** Verificar que CAJERO también puede registrar platos servidos (PA-111).
  **Pre-condición:** `cajeroToken` disponible. Notificación seed ACTIVA PLATOS_LISTOS id=17.
  **Resultado esperado:** 200 OK.
url: "{{baseUrl}}/api/notificaciones/17/servir-platos"
method: PATCH
headers:
  Authorization: Bearer {{cajeroToken}}
scripts:
  - type: afterResponse
    code: |-
      pm.test('HTTP 200', () => pm.response.to.have.status(200));
    language: text/javascript
order: 7108
