$kind: http-request
name: CD7-02 MESERO borra notas – 200 OK
description: |-
  **Objetivo:** Verificar que `notas = null` borra las notas existentes.
  **Pre-condición:** beforeRequest auto-provisiona la comanda y guarda su id en `cd702ComandaId`.
  **Resultado esperado:** 200 OK; `notasCocina` en la respuesta es null.
url: "{{baseUrl}}/api/comandas/borrador/{{cd702ComandaId}}/notas"
method: PATCH
headers:
  Authorization: Bearer {{meseroToken}}
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "notas": null
    }
scripts:
  - type: beforeRequest
    code: |-
      const unique = 'E7-CD7-02-' + Date.now();
      pm.sendRequest({
        url: pm.environment.get('baseUrl') + '/api/auth/login',
        method: 'POST',
        header: { 'Content-Type': 'application/json' },
        body: { mode: 'raw', raw: JSON.stringify({
          email: pm.environment.get('emailMesero'),
          password: pm.environment.get('passwordValida'),
          forceSessionOverride: true
        }) }
      }, (err1, res1) => {
        if (err1 || !res1 || res1.code !== 200) return;
        const token = res1.json().accessToken;
        pm.environment.set('meseroToken', token);

        pm.sendRequest({
          url: pm.environment.get('baseUrl') + '/api/comandas/borrador/items',
          method: 'POST',
          header: { 'Authorization': 'Bearer ' + token, 'Content-Type': 'application/json' },
          body: { mode: 'raw', raw: JSON.stringify({
            visitaId: 14,
            productoId: Number(pm.environment.get('productoTestPlatoId')),
            cantidad: 1,
            descripcion: unique
          }) }
        }, (err2, res2) => {
          if (err2 || !res2 || res2.code !== 200) return;
          const comandaId = res2.json().data.comandaCocinaId;
          if (comandaId) pm.environment.set('cd702ComandaId', comandaId);
        });
      });
    language: text/javascript
  - type: afterResponse
    code: |-
      pm.test('El sistema retorna HTTP 200', function () {
        pm.response.to.have.status(200);
      });

      pm.test('notasCocina queda en null', function () {
        const data = pm.response.json().data;
        pm.expect(data.notasCocina).to.equal(null);
      });
    language: text/javascript
order: 1010
