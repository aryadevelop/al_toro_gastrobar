$kind: http-request
name: 70-04 Atender Cambio – MESERO
description: Atender notificación de cambio (CA-06). Devuelve comandaId.
url: "{{baseUrl}}/api/notificaciones/{{tmpNotificacionId}}/atender-cambio"
method: PATCH
headers:
  Authorization: Bearer {{tmpMeseroToken}}
scripts:
  - type: beforeRequest
    code: |-
      // Ajustar al ID de una notificación CAMBIO activa con comanda asociada
      pm.environment.set('tmpNotificacionId', '3');

      // Login autónomo con credenciales hardcoded
      pm.sendRequest({
        url: '{{baseUrl}}/api/auth/login',
        method: 'POST',
        header: { 'Content-Type': 'application/json' },
        body: {
          mode: 'raw',
          raw: JSON.stringify({
            email: 'mesero1@altoro.com',
            password: 'password123',
            forceSessionOverride: true
          })
        }
      }, (err, res) => {
        if (!err && res && res.code === 200) {
          pm.environment.set('tmpMeseroToken', res.json().accessToken);
        }
      });
    language: text/javascript
  - type: afterResponse
    code: |-
      pm.environment.unset('tmpNotificacionId');
      pm.environment.unset('tmpMeseroToken');
    language: text/javascript
order: 7040
