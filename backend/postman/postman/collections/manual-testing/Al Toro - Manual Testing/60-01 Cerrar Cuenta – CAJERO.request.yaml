$kind: http-request
name: 60-01 Cerrar Cuenta – CAJERO
description: Registrar venta y cerrar cuenta de una visita (incluye todos los campos del DTO)
url: "{{baseUrl}}/api/ventas"
method: POST
headers:
  Authorization: Bearer {{tmpCajeroToken}}
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "emailCajero": "cajero1@altoro.com",
      "visitaId": 1,
      "subtotal": 85000.00,
      "descuento": 5000.00,
      "total": 80000.00,
      "metodo": "EFECTIVO"
    }
scripts:
  - type: beforeRequest
    code: |-
      // Login autónomo con credenciales hardcoded
      pm.sendRequest({
        url: '{{baseUrl}}/api/auth/login',
        method: 'POST',
        header: { 'Content-Type': 'application/json' },
        body: {
          mode: 'raw',
          raw: JSON.stringify({
            email: 'cajero1@altoro.com',
            password: 'password123',
            forceSessionOverride: true
          })
        }
      }, (err, res) => {
        if (!err && res && res.code === 200) {
          pm.environment.set('tmpCajeroToken', res.json().accessToken);
        }
      });
    language: text/javascript
  - type: afterResponse
    code: |-
      pm.environment.unset('tmpCajeroToken');
    language: text/javascript
order: 6010
