$kind: http-request
name: 20-08 Detalle Reserva – CLIENTE
description: Obtener detalle completo de una reserva
url: "{{baseUrl}}/api/reservas/{{tmpReservaId}}/detalle"
method: GET
headers:
  Authorization: Bearer {{tmpClienteToken}}
scripts:
  - type: beforeRequest
    code: |-
      // Configurar ID temporal (usar ID conocido)
      pm.environment.set('tmpReservaId', '10');

      // Login autónomo con credenciales hardcoded
      pm.sendRequest({
        url: '{{baseUrl}}/api/auth/login',
        method: 'POST',
        header: { 'Content-Type': 'application/json' },
        body: {
          mode: 'raw',
          raw: JSON.stringify({
            email: 'cliente1@altoro.com',
            password: 'password123',
            forceSessionOverride: true
          })
        }
      }, (err, res) => {
        if (!err && res && res.code === 200) {
          pm.environment.set('tmpClienteToken', res.json().accessToken);
        }
      });
    language: text/javascript
  - type: afterResponse
    code: |-
      pm.environment.unset('tmpReservaId');
      pm.environment.unset('tmpClienteToken');
    language: text/javascript
order: 2080
