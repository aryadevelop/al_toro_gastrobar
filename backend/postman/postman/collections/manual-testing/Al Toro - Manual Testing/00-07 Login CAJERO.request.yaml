$kind: http-request
name: 00-07 Login CAJERO
description: Login manual como cajero para obtener tokens
url: "{{baseUrl}}/api/auth/login"
method: POST
headers:
  Content-Type: application/json
body:
  mode: raw
  raw: |-
    {
      "email": "{{emailCajero}}",
      "password": "{{passwordValida}}",
      "forceSessionOverride": true
    }
scripts:
  - type: afterResponse
    code: |-
      // Guardar token en ambiente
      if (pm.response.code === 200) {
        const body = pm.response.json();
        pm.environment.set('cajeroToken', body.accessToken);
        console.log('✓ Login exitoso - token guardado en cajeroToken');
      } else {
        console.warn('✗ Login falló con código', pm.response.code);
      }
    language: text/javascript
order: 700
