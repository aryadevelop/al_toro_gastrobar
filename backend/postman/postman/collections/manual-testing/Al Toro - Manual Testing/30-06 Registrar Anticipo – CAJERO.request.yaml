$kind: http-request
name: 30-06 Registrar Anticipo – CAJERO
description: |-
  Registra un anticipo sobre una reserva ESPECIAL CONFIRMADA como CAJERO.
  Cambiar tmpReservaId al ID de la reserva a abonar (debe existir en estado CONFIRMADA).
  El monto no puede superar el pendiente máximo (totalReserva - totalAnticipado).
url: "{{baseUrl}}/api/reservas/{{tmpReservaId}}/abonos"
method: POST
headers:
  Authorization: Bearer {{tmpCajeroToken}}
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "tipo": "ANTICIPO",
      "monto": 50000,
      "metodo": "EFECTIVO",
      "fechaHora": "2026-05-23T10:00:00"
    }
scripts:
  - type: beforeRequest
    code: |-
      // Ajustar el ID de reserva según la prueba manual (debe estar en estado CONFIRMADA)
      pm.environment.set('tmpReservaId', '10');

      pm.sendRequest({
        url: '{{baseUrl}}/api/auth/login',
        method: 'POST',
        header: { 'Content-Type': 'application/json' },
        body: {
          mode: 'raw',
          raw: JSON.stringify({
            email: 'cajero2@altoro.com',
            password: 'Al.Toro2026!',
            forceSessionOverride: true
          })
        }
      }, (err, res) => {
        if (!err && res && res.code === 200) {
          pm.environment.set('tmpCajeroToken', res.json().accessToken);
        }
      });
    language: text/javascript
  - type: afterResponse
    code: |-
      pm.environment.unset('tmpReservaId');
      pm.environment.unset('tmpCajeroToken');
    language: text/javascript
order: 3060
