$kind: http-request
name: 00-08 Login ADMIN
description: Login manual como administrador para obtener tokens
url: "{{baseUrl}}/api/auth/login"
method: POST
headers:
  Content-Type: application/json
body:
  mode: raw
  raw: |-
    {
      "email": "{{emailAdmin}}",
      "password": "{{passwordValida}}",
      "forceSessionOverride": true
    }
scripts:
  - type: afterResponse
    code: |-
      // Guardar token en ambiente
      if (pm.response.code === 200) {
        const body = pm.response.json();
        pm.environment.set('adminToken', body.accessToken);
        console.log('✓ Login exitoso - token guardado en adminToken');
      } else {
        console.warn('✗ Login falló con código', pm.response.code);
      }
    language: text/javascript
order: 800
