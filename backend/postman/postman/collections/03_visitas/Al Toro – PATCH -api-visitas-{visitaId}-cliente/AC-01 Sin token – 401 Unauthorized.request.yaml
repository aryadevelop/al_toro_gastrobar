$kind: http-request
name: AC-01 Sin token – 401 Unauthorized
url: "{{baseUrl}}/api/visitas/15/cliente?clienteId=11"
method: PATCH
queryParams:
  clienteId: "11"
scripts:
  - type: afterResponse
    code: |-
      pm.test('Sin token → 401', function () {
        pm.response.to.have.status(401);
      });
    language: text/javascript
order: 1000
